using UnityEngine;

public class rootLock : MonoBehaviour
{
    private void LateUpdate()
    {
        //if (isGrabbed)
        //{
        //    Vector3 rot = transform.eulerAngles;

        //    transform.rotation =
        //        Quaternion.Euler(0f, rot.y, 0f);
        //}
    }
}
